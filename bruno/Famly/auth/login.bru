meta {
  name: login
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/v1/auth/login
  body: json
  auth: inherit
}

body:json {
  {
    "email": "user@example.com",
    "password": "securePassword123"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
