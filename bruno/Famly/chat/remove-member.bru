meta {
  name: Remove Member
  type: http
  seq: 6
}

delete {
  url: {{baseUrl}}/v1/chats/{{currentChatId}}/members/{{otherUserId}}
  auth: inherit
}

headers {
  Authorization: Bearer {{accessToken}}
}

settings {
  encodeUrl: true
  timeout: 0
}
