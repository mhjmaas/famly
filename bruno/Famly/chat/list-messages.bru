meta {
  name: List Messages
  type: http
  seq: 8
}

get {
  url: {{baseUrl}}/v1/chats/{{currentChatId}}/messages
  auth: inherit
}

headers {
  Authorization: Bearer {{accessToken}}
}

query {
  limit: 50
}

settings {
  encodeUrl: true
  timeout: 0
}
