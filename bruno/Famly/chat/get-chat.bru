meta {
  name: Get Chat
  type: http
  seq: 4
}

get {
  url: {{baseUrl}}/v1/chats/{{currentChatId}}
  auth: inherit
}

headers {
  Authorization: Bearer {{accessToken}}
}

settings {
  encodeUrl: true
  timeout: 0
}
