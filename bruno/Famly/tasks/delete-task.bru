meta {
  name: delete task
  type: http
  seq: 6
}

delete {
  url: {{baseUrl}}/v1/families/{{currentFamilyId}}/tasks/{{currentTaskId}}
  body: none
  auth: inherit
}

settings {
  encodeUrl: true
  timeout: 0
}
